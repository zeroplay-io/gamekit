'use strict';

const gamekitFeatures = require('..');
const assert = require('assert').strict;

assert.strictEqual(gamekitFeatures(), 'Hello from gamekitFeatures');
console.info('gamekitFeatures tests passed');
